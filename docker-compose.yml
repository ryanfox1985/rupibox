web:
  build: .
  command: ./start.sh
  ports:
    - "3000:3000"
  environment:
    RAILS_ENV: production
    RAILS_SERVE_STATIC_FILES: true
    SECRET_KEY_BASE: <%= rake secret %>
  volumes:
   - /sys:/sys
