<%= form_for(@pin) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :pin_pi %><br>
    <%= f.number_field :pin_pi %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.check_box :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
